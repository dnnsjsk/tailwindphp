<?php

declare(strict_types=1);

namespace TailwindPHP;

/**
 * CSS Functions
 *
 * Port of: packages/tailwindcss/src/css-functions.ts
 *
 * Handles CSS function substitution for:
 * - --alpha(color / opacity)
 * - --spacing(multiplier)
 * - --theme(path)
 * - theme(path) (legacy)
 *
 * TODO: Full implementation needed for complete CSS function support.
 */
